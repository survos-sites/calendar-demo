> # ⛔ DEPRECATED — do not use
>
> Superseded by **[ccal](https://github.com/survos/ccal)** (the Community Calendar Aggregator app) + **[survos/ux-calendar-bundle](https://github.com/survos/ux-calendar-bundle)** (FullCalendar v7 + AssetMapper rendering).
>
> An early MVP built on the tattali `dev-tac` fork. Superseded by ccal.

---

A very basic Symfony 7 app that integrates calendar bundle.  MVP

```bash
git clone git@github.com:survos-sites/calendar-demo.git 
cd calendar-demo
composer install
symfony server:start -d
symfony open:local
```